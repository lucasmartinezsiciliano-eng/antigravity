import BuscadorCliente from "@/components/Buscador";

export const metadata = {
  title: "Buscar coches en toda Europa — Coche Europa",
};

export default function BuscarPage() {
  return <BuscadorCliente />;
}
